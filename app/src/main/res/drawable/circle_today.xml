<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="2dp"
        android:color="@color/app_color" /> <!-- Changez la couleur ici -->
    <corners android:radius="360dp" /> <!-- Cercle parfait -->
</shape>

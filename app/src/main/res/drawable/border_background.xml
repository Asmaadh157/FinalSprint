<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Couleur de fond (facultatif) -->
    <stroke
        android:width="2dp"
        android:color="@color/button_color" /> <!-- Couleur et largeur de la bordure -->
    <corners android:radius="8dp" /> <!-- Coins arrondis -->
    <padding
        android:left="5dp"
        android:top="5dp"
        android:right="5dp"
        android:bottom="5dp" />
</shape>
